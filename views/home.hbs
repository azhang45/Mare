<!DOCTYPE html>
<html>

<head>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
</div>
    <form action="/upload" method="POST" enctype="multipart/form-data">
        <!-- The file upload input -->
        <input type="file" name="img" class="upload-img" />
        <!-- The submit button -->
        <button class="button-35" type="submit" id="submit">Submit</button>
        <button class="button-35" type="convert" id="convert">Convert</button>
    </form>
    {{#if imgPath}}
    <img style="max-width: 50%" src="{{imgPath}}">
    <a href="{{imgPath}}" download="img">Download</a>
    {{/if}}


    {{!-- <img style="max-width: 50%" src="<%= typeof file != 'undefined' ? file : ''%>"> --}}

    <p style="color:red">{{msg}}</p>
</body>

</html>
